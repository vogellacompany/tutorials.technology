<section id="tomcat_httpserver">
	<title>Tomcat as HTTP Server</title>
	<para>
		contains also a HTTP connector which can be used
		to serve static HTML
		pages. The standard directory which will be
		served is below the
		<wordasword>Tomcat</wordasword>

		<filename>webapps/ROOT</filename>
		below the installation directory. Place static content
		into this directory.
	</para>
	<para>
		To allow directory browsing via Apache Tomcat change the
		<parameter>listings</parameter>
		parameter
		in the file
		<filename>conf/web.xml</filename>
		from false to true.
		<para>
			<programlisting>
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="../examples/httpserver/listings.txt" />
			</programlisting>
		</para>
	</para>


</section>