<section id="groovybuilder">
== Groovy builders
	
		Groovy supports the builder pattern to create tree-like data
		structures. The base class for the builder support is
		`BuilderSupport`
		and its subclasses are
		`NodeBuilder`
		,
		`MarkupBuilder`
		,
		`AntBuilder`
		and
		`SwingBuilder`
		.
	
