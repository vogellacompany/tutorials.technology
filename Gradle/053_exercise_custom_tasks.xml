<section id="exercise_custom_gradle_tasks">
	<title>Exercise: Create Custom Gradle Tasks</title>
	<section id="exercise_custom_wrapper_task">
		<title>Exercise: Specifying a Wrapper Task</title>
		<para>Gradle's Wrapper Task can be implemented like this:</para>
		<para>
			<programlisting language="gradle">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/wrapper/wrappertask.gradle" />
			</programlisting>
		</para>
	</section>
	<section id="exercise_custom_copy_task">
		<title>Exercise: Creating a Copy Task</title>
		<para>The Copy task type can be used to specify a task, which is able
			to copy files.
		</para>
		<para>
			Create a new project with the following
			<filename>build.gradle</filename>
			file:
		</para>
		<para>
			<programlisting language="gradle">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/tasks/copy_task_type.gradle" />
			</programlisting>
		</para>
		<para>
			Create a
			<filename>source</filename>
			folder inside this project and add a text file to the
			<filename>source</filename>
			folder.
		</para>
		<para>
			When running the
			<code>copyFile</code>
			task it copies the text file to a new
			<filename>destination</filename>
			folder.
		</para>
	</section>
	<section id="exercise_custom_external_file_task">
		<title>Exercise: Specifying a custom Task in another gradle file
		</title>
		<para>Create a new Gradle project, which contains the following
			structure.
		</para>
		<para>
			<mediaobject>
				<imageobject>
					<imagedata fileref="images/buildSrc_Tasks.png" />
				</imageobject>
			</mediaobject>
		</para>
		<para>
			The
			<filename>CheckWebsite.groovy</filename>
			class looks like this:
		</para>
		<para>
			<programlisting language="gradle">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/tasks/CheckWebsite.groovy" />
			</programlisting>
		</para>
		<para>
			Since this class has external dependencies to jsoup, a
			<filename>build.gradle</filename>
			file for this class has to be created.
		</para>
		<para>
			So the
			<filename>build.gradle</filename>
			inside the
			<filename>buildSrc</filename>
			folder, which is responsible for building the
			<code>CheckWebsite</code>
			class, looks like this:
		</para>
		<para>
			<programlisting language="gradle">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/tasks/buildSrcCheckWebsite.gradle" />
			</programlisting>
		</para>
		<para>
			Finally the main
			<filename>build.gradle</filename>
			file in the root folder makes use of the new
			<code>com.example.CheckWebsite</code>
			task type.
		</para>
		<para>
			<programlisting language="gradle">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/tasks/Checkwebsite.gradle" />
			</programlisting>
		</para>
	</section>
</section>
