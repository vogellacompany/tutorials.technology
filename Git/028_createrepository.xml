<section id="firstgit" role="wrapper">
	<title>Create repository</title>

	<section id="firstgit_createrepotarget">
		<title>Target of this chapter</title>
		<para>
			In this chapter you create a local Git
			repository. The comments
			(marked with #) before
			the
			commands
			explain
			the specific actions.
		</para>
		<para>
			Open a command
			shell for
			the operations.
		</para>
	</section>
	<section id="firstgit_directory">
		<title>Create a directory</title>
		<para>
			The following commands create an empty directory which is used
			later in this exercise to contain the working tree and the
			Git
			repository.
		</para>
		<para>
			<programlisting language="bourne">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/commandline/gitrepo_createdirectory" />
			</programlisting>
		</para>
	</section>
	<section id="firstgit_repository_creation">
		<title>Create a new Git repository</title>
		<indexterm>
			<primary>Repository</primary>
			<secondary>create</secondary>
		</indexterm>
		<indexterm>
			<primary>Create Git repository</primary>
		</indexterm>
		<para>
			The following explanation is based on a non-bare repository. See
			<xref linkend="gitterminology" />
			for the difference between a bare repository and a non-bare
			repository with a
			<wordasword>working tree</wordasword>
			.
		</para>
		<para>
			Every Git repository is stored in the
			<filename class="directory">.git</filename>
			folder of the
			directory in which the Git repository has been created.
			This
			directory
			contains the complete history of the
			repository.
			The
			<filename>.git/config</filename>
			file
			contains the
			configuration for the
			repository.
		</para>
		<para>
			The following command
			creates a Git repository in the current
			directory.
		</para>
		<para>
			<programlisting language="bourne">
				<xi:include xmlns:xi="http://www.w3.org/2001/XInclude"
					parse="text" href="./examples/commandline/gitrepo_create" />
			</programlisting>
		</para>
		<para>
			All files inside the repository folder excluding the
			<filename>.git</filename>
			folder are the
			<wordasword>working tree</wordasword>
			for a Git repository.
		</para>
	</section>
</section>
