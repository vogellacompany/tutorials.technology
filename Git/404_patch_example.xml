<section id="gitpatch_create" role="wrapper">
	<title>Example: Creating patches</title>
	<section id="gitpatch_createandapply">
		<title>Create and apply patches</title>
		
			LARSPRIMARYINDEX"Create patch"LARSPRIMARYINDEX
		
		
			LARSPRIMARYINDEX"git format-patch"LARSPRIMARYINDEX
		
		<para>
			The following example
			creates a branch, changes several files and
			creates a commit recording these changes.
		</para>
		[source,terminal]
----
include::./examples/patch/patch1.txt[]
----
		<para>The next example creates a patch for these changes. </para>
		[source,terminal]
----
include::./examples/patch/patch2.txt[]
----

		<para>
			To apply this patch to your master branch in a different clone of the
			repository, switch to it and use the
			<code>git apply</code>
			command.
		</para>
		[source,terminal]
----
include::./examples/patch/patch3.txt[]
----

		<para>Afterwards you can commit the changes introduced by the patches
			and delete the patch file.
		</para>
		[source,terminal]
----
include::./examples/patch/patch4.txt[]
----

		<tip>
			<para>
				Use the
				<code>git am</code>
				command to apply and commit the changes in a single step. To apply
				and commit all patch files in the directory use, for example, the
				<code>git am *.patch</code>
				command. You specify the order in which the patches are applied by
				specifying them on the command
				line.
			</para>
		</tip>

	</section>
	<section id="gitpatch_createforcommit">
		<title>Create a patch for a selected commit</title>
		<para>
			You can specify the commit ID and the number of patches
			which should be created. For example, to create a patch for selected
			commits based on the HEAD
			pointer you can use the following commands.
		</para>
		[source,terminal]
----
include::./examples/patch/patch5.txt[]
----

	</section>

</section>
