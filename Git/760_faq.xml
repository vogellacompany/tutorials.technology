<section id="gitfaq">
	<title>Frequently asked questions</title>
	<indexterm>
		<primary>FAQ</primary>
	</indexterm>
	<section id="gitfaq_symlinks">
		<title>Can Git handle symlinks?</title>
		<indexterm>
			<primary>symlinks</primary>
		</indexterm>
		<para>The usage of symlinks requires that the operating
			system used by
			the
			developers supports
			them.
		</para>
		<para>
			Git as version control system can handle symlinks.
		</para>
		<para>
			If the
			symlink points to a file, then Git stores the
			path
			information it is symlinking to, and the file type. This is similar
			to a symlink to a directory; Git does
			not store the
			contents under the
			symlinked
			directory.
		</para>
	</section>

</section>