<section id="svn_migration">
	<title>Migrating from SVN</title>
	
		LARSPRIMARYINDEX"svn"LARSPRIMARYINDEX
	
	
		LARSPRIMARYINDEX"migration"LARSPRIMARYINDEX
	
	<para>
		To convert Subversion projects to Git you can use a RubyGem
		called <wordasword>svn2git</wordasword>
		which relies on <code>git svn</code> internally
		and handles most of the trouble.
	</para>

	<para>To install it (on Ubuntu) simply type:</para>
	[source,terminal]
----
include::./examples/migration/svn2git_install.txt[]
----

	<para>
		Let's say you have a repository called
		<code>http://svn.example.com/repo</code>
		with the default layout (trunk, branches, tags)
		and already prepared a local git repository where you want to put everything,
		then navigate to your git directory
		and use the following commands:
	</para>
	[source,terminal]
----
include::./examples/migration/svn2git_example.txt[]
----
	<para>
		The parameter <parameter>--verbose</parameter> adds detailed output to the
		commandline so you can see what is going on including potential errors.
		The second <code>svn2git --rebase</code> command aligns your new git
		repository with the svn import. You are now ready to push to the web and get forked!
		If your svn layout deviates from the standard or other problems occur,
		seek <code>svn2git --help</code> for documentation on additional parameters.
	</para>

</section>