[[revertcommit]]
= Revert commits

[[undochanges_revertcommit1]]
== Reverting a commit
((git revert))
You can revert commits via
the `git revert` command. This command reverts the changes of a commit.

Such commits are useful to document that a change was withdrawn.

[[undochanges_revertcommit2]]

== Example: Reverting a commit

The following command demonstrates the usage of the `git revert`
command.

[source,console]
----
include::./res/commandline/git_revert_commit[]
----

