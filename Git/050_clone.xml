<section id="gitclone" role="wrapper">
	<title>Cloning repositories and the remote called "origin"</title>
	<section id="remotes_cloneoperation">
		<title>Cloning a repository</title>
		
			LARSPRIMARYINDEX"Repository"LARSPRIMARYINDEX
			LARSSECONDARY"clone"LARSSECONDARY
		

		
			LARSPRIMARYINDEX"git clone"LARSPRIMARYINDEX
		

		<para>
			The
			<code>git clone</code>
			command copies an existing Git repository. This copy is a
			working Git
			repository with the complete history of the cloned repository. It
			can
			be used completely isolated from
			the
			original
			repository.
		</para>

	</section>
	<section id="remotes_clone_origin">
		<title>The remote called "origin"</title>
		
			LARSPRIMARYINDEX"origin"LARSPRIMARYINDEX
			LARSSECONDARY""LARSSECONDARY
		
		<para>
			If you clone a repository, Git implicitly creates a
			<wordasword>remote</wordasword>
			named
			<wordasword>origin</wordasword>
			by default. The
			<wordasword>origin</wordasword>
			<wordasword>remote</wordasword>
			links back to the cloned repository.
		</para>
		<para>
			If you
			create a Git repository from
			scratch with the
			<code>git init</code>
			command, the
			<wordasword>origin</wordasword>
			remote is not created automatically.
		</para>

	</section>



	<section id="remotes_setupexercise">
		<title>Exercise: Cloning to create a bare Git repository</title>

		<para>
			In this section you create a bare Git repository.
			In
			order
			to
			simplify the following examples, the Git
			repository
			is
			hosted
			locally
			in
			the filesystem and not on a server in the Internet.
		</para>


		<para>Execute the following commands to create a bare repository based
			on your existing Git repository.
		</para>
		[source,terminal]
----
include::./examples/commandline/git_remote.commands[]
----
		<tip>
			<para>
				If you receive a warning similar to the following:
				<code> push.default is unset; its implicit
					value
					is changing in
					Git 2.0
					from 'matching' to 'simple'</code>, see
				<xref linkend="setup_pushconfiguration" />
				for the missing configuration.
			</para>
		</tip>

	</section>


</section>
